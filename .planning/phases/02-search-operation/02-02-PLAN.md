---
phase: 02-search-operation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05

user_setup: []

must_haves:
  truths:
    - "node outlook.js search 'from:<real-sender>' returns results with non-null ids against live Outlook"
    - "node outlook.js search '<gibberish>' returns empty results with count 0 — not an error"
    - "node outlook.js search '<query>' --limit 5 returns at most 5 results"
    - "Each result has the 8-field schema: id, subject, from, to, date, preview, is_read, is_flagged"
    - "The session is detected as valid (no SESSION_INVALID error) when a live session exists"
  artifacts: []
  key_links:
    - from: "outlook.js search"
      to: "live Outlook instance"
      via: "agent-browser combobox workflow"
      pattern: "search.*results"
---

<objective>
Verify the search subcommand works end-to-end against a live Outlook instance. This is a blocking human verification checkpoint — the user must confirm that search results are returned with correct JSON structure, non-null IDs, and correct field parsing before Phase 2 is considered complete.

Purpose: Browser automation against a live SPA is inherently fragile. The ARIA selectors, combobox timing, eval output format, and content boundary behavior were all researched in Phase 0 but some assumptions (A1-A5 in RESEARCH.md) were untested. This checkpoint validates the full chain.

Output: Human-verified confirmation that search works against live Outlook, or a list of issues to fix.
</objective>

<execution_context>
@/Users/b.g.albert/outlook-web-skill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/b.g.albert/outlook-web-skill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-search-operation/02-CONTEXT.md
@.planning/phases/02-search-operation/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live search verification against real Outlook</name>
  <files>n/a — checkpoint only, no files modified</files>
  <read_first>
    - /Users/b.g.albert/outlook-web-skill/.planning/phases/02-search-operation/02-CONTEXT.md (expected behavior from decisions)
    - /Users/b.g.albert/outlook-web-skill/.planning/phases/02-search-operation/02-RESEARCH.md (assumptions A1-A5 to validate)
  </read_first>
  <action>
Present the 5 verification tests to the user and wait for confirmation. If any test fails, diagnose the issue by reading stderr output and the relevant source files, then apply a fix and re-test. The verification tests are:

**Pre-requisite:** Ensure the user has an active Outlook session. If needed, run `node outlook.js auth` with the appropriate env vars.

**Test 1: Basic search with known sender.** Run `node outlook.js search "from:<a-real-sender-email>"` with env vars set. Expected: JSON with `"status":"ok"`, `"results":[...]` containing items with all 8 schema fields. The `id` fields should be non-null base64 strings. `to` and `is_flagged` should be `null`.

**Test 2: Zero results.** Run `node outlook.js search "from:zzz-nonexistent-person-xyz-12345@fake.example"`. Expected: `{"operation":"search","status":"ok","results":[],"count":0,"error":null}`.

**Test 3: Limit parameter.** Run `node outlook.js search "is:unread" --limit 3`. Expected: At most 3 results returned.

**Test 4: Missing query.** Run `node outlook.js search` (no query argument). Expected: `{"operation":"search","status":"error","error":{"code":"INVALID_ARGS",...}}`.

**Test 5: KQL operators.** Run `node outlook.js search "has:attachment"`. Expected: Results with consistent JSON structure.

If failures occur, check stderr for: content boundary markers wrapping JSON (Pitfall 3), eval ID count mismatch warnings (Pitfall 1), snapshot parse failures, or combobox interaction errors. Apply targeted fixes based on the specific failure mode.
  </action>
  <verify>Human confirms all 5 tests pass by typing "approved" or describes failures.</verify>
  <acceptance_criteria>
    - Test 1 returns status ok with non-empty results array
    - Test 1 results have non-null id fields (base64 strings like "AQAAAAAJuqMCAAAIQYb16wAAAAA=")
    - Test 1 results have is_read as boolean (true or false), to as null, is_flagged as null
    - Test 2 returns status ok with empty results array and count 0
    - Test 3 returns at most 3 results
    - Test 4 returns status error with code INVALID_ARGS
    - Test 5 returns results with consistent JSON structure
    - No crashes or unhandled exceptions in any test
  </acceptance_criteria>
  <done>User has confirmed all 5 live search tests pass, or all failures have been diagnosed and fixed until all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester -> live Outlook | Real email data is accessed during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-07 | Information Disclosure | Real email metadata in test output | accept | This is a human verification step on the user's own machine with their own email. The JSON output contains email metadata (sender, subject, preview) which the user already has access to. No mitigation needed — this is the intended use case. |
</threat_model>

<verification>
Human confirms all 5 tests pass with expected output structure.
</verification>

<success_criteria>
- All 5 manual tests produce the expected JSON output
- Exchange ItemIDs (id field) are non-null for results that exist
- Zero-results query returns empty array, not an error
- --limit constrains result count
- Missing argument returns INVALID_ARGS
- No crashes or partial JSON output
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-operation/02-02-SUMMARY.md`
</output>
