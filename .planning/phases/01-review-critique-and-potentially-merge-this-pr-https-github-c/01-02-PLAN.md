---
phase: 01-review-critique-and-potentially-merge-this-pr
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - README.md
  - SKILL.md
  - CLAUDE.md
autonomous: false

requirements: []

must_haves:
  truths:
    - "README.md documents teams and copilot-summary subcommands with usage examples and response shapes"
    - "SKILL.md lists teams and copilot-summary with trigger descriptions and response examples"
    - "SKILL.md error codes table includes teams and copilot-summary"
    - "README.md project structure lists teams.js, copilot.js, policy-teams.json, policy-copilot.json"
    - "CLAUDE.md description mentions Teams and Copilot capability"
    - "Feature branch is merged to main"
    - "PR #1 is closed"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation with teams/copilot subcommand docs"
      contains: "copilot-summary"
    - path: "SKILL.md"
      provides: "Skill descriptor with teams/copilot trigger words and subcommand reference"
      contains: "copilot-summary"
    - path: "CLAUDE.md"
      provides: "Project description updated to mention Teams/Copilot"
      contains: "Teams"
  key_links:
    - from: "SKILL.md"
      to: "outlook.js"
      via: "subcommand documentation"
      pattern: "node outlook.js teams"
    - from: "README.md"
      to: "outlook.js"
      via: "subcommand documentation"
      pattern: "node outlook.js copilot-summary"
---

<objective>
Update all project documentation (README.md, SKILL.md, CLAUDE.md) to describe the new teams and
copilot-summary subcommands, then merge the feature branch to main and close PR #1 with a reference.

Purpose: Complete the phase by ensuring both user-facing docs and skill descriptor reflect all
subcommands that now exist (per CLAUDE.md convention: "Both files must describe every subcommand
that exists after the phase").

Output: Updated docs committed, feature branch merged, PR #1 closed.
</objective>

<execution_context>
@/Users/username/outlook-web-skill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/username/outlook-web-skill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-CONTEXT.md
@.planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-RESEARCH.md
@.planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md, SKILL.md, and CLAUDE.md with teams/copilot documentation, then merge and close PR</name>
  <read_first>
    - README.md (current state on feature branch — see existing subcommand doc sections, project structure, limitations, error codes)
    - SKILL.md (current state on feature branch — see existing subcommand reference, error codes table, description frontmatter)
    - CLAUDE.md (current state on feature branch — see project description and core value statement)
    - .planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-RESEARCH.md (see exact PR diff for README/SKILL/CLAUDE changes the PR proposed — use as template but adapt for our changes)
    - .planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-01-SUMMARY.md (what was done in plan 01)
  </read_first>
  <files>README.md, SKILL.md, CLAUDE.md</files>
  <action>
**Step A: Update README.md**

Make these changes to README.md (the feature branch `feat/teams-copilot-merge` should be checked out):

1. In the "What it can do:" bullet list, add two bullets after "Recalibrate the scoring algorithm when rankings seem off":
   - `- Fetch Microsoft Teams activity feed, @mentions, and unread chats`
   - `- Get AI-generated summaries of emails and Teams messages from Copilot in Teams`

2. Add a `### teams` section after the `tune` section (after the `---` separator following tune docs). Use the PR's documentation content (from the PR diff) as the template. The section should include:
   - Usage: `node outlook.js teams [--mentions] [--unread] [--limit <n>]`
   - Three modes: default (activity feed), `--mentions` (@mentions only), `--unread` (unread chats)
   - Example response JSON for activity/mentions mode with fields: sender, channel, preview, time, type
   - Type values: mention, reply, reaction, message, notification
   - Example response for `--unread` mode with fields: name, preview, time, has_unread
   - Note about TEAMS_BASE_URL env var and same Entra SSO session

3. Add a `### copilot-summary` section after the `teams` section. Include:
   - Usage: `node outlook.js copilot-summary [--type email|teams|both] [--since "<date>"] [--prompt "<custom>"]`
   - Modes: email, teams, both (default)
   - Example response JSON with fields: operation, status, type, raw_response, items (sender/subject/date/summary/action), count, error
   - Critical notes: raw_response always present, items is best-effort parse, 10-30s generation time, Researcher plugin auto-removal, ~25 item truncation limit

4. In the Error Codes table, update the "When emitted" column:
   - `SESSION_INVALID` row: change to `search, read, digest, teams, copilot-summary`
   - `OPERATION_FAILED` row: change to `read, digest, auth, teams, copilot-summary`

5. In the Limitations section, add two bullets:
   - `- **Teams ARIA** — Teams web's accessibility tree varies by version. The \`teams\` subcommand extracts what it can from listitem/option elements; some activity items may not parse cleanly.`
   - `- **Copilot availability** — \`copilot-summary\` requires Copilot to be enabled for your Teams tenant. Generation takes 10-30 seconds and Copilot may truncate at ~25 items.`

6. In the Limitations section, update the existing multi-tenant bullet to mention Teams:
   - Change `tested on a single Outlook tenant` to `tested on a single Outlook/Teams tenant`

7. In the Project Structure section, add after `tune.js`:
   ```
     teams.js              Teams activity feed, mentions, unread chats
     copilot.js            Copilot-in-Teams summary generation
   ```
   And update the policy file listing:
   - Keep `policy-auth.json` and `policy-read.json` as-is
   - Change `policy-search.json` description to: `Action allowlist for search/digest operations`
   - Add: `policy-teams.json        Action allowlist for teams operations`
   - Add: `policy-copilot.json      Action allowlist for copilot-summary operations`

**Step B: Update SKILL.md**

1. Update the frontmatter `description` field to mention Teams and Copilot. Replace the current description with:
   ```
   description: Read-only access to Outlook web email, calendar, and Microsoft Teams. Use when the user needs to search emails, read an email, get a prioritized digest of today's inbox, check calendar events, find a specific meeting, check Teams activity/mentions, or get a Copilot-generated summary. Triggers include "check my email", "find email from", "read that email", "what's in my inbox today", "what's on my calendar", "do I have any meetings today", "check Teams", "Teams mentions", "copilot summary", or any request to access, search, or summarize Outlook email, calendar, or Teams messages. NEVER use for sending, replying, deleting, accepting, declining, or any write operation.
   ```

2. Update the opening paragraph to mention Teams:
   ```
   Reads the user's Outlook web inbox, calendar, and Microsoft Teams via a managed Chrome session — no Microsoft Graph API, no app registration. Invoke via `node outlook.js <subcommand>` from the skill root directory. This skill will NEVER send, delete, move, reply to, forward, flag, unflag, accept, or decline any email, Teams message, or calendar item.
   ```

3. Add a `### teams` subcommand reference section after the existing `tune` section. Include the same content as the README teams section but in the SKILL.md format (concise, focused on the calling LLM):
   - Usage with flags
   - Mode descriptions (activity default, --mentions, --unread)
   - Response JSON example for activity mode
   - Response JSON example for unread mode
   - Type values
   - Note about Entra SSO and TEAMS_BASE_URL

4. Add a `### copilot-summary` subcommand reference section. Include:
   - Usage with flags
   - Mode descriptions (--type, --since, --prompt)
   - Response JSON example
   - Critical notes (raw_response, items best-effort, 10-30s, Researcher plugin, ~25 truncation)

5. Update the Error Codes table:
   - `SESSION_INVALID` "When emitted": `search, read, digest, teams, copilot-summary`
   - `OPERATION_FAILED` "When emitted": `read, digest, auth, teams, copilot-summary`

**Step C: Update CLAUDE.md**

1. Update the project description paragraph (first paragraph under `## Project`) to mention Teams:
   ```
   A Claude Code skill (or set of skills) that gives a personal assistant Claude Code instance read-only access to Outlook web email, calendar, and Microsoft Teams. It uses the `@vercel/agent-browser` npm package to control a browser via CLI, navigates to the Outlook and Teams web UIs, handles authentication state detection, and returns structured JSON data to the calling agent. Also supports getting AI-generated summaries from Copilot in Teams.
   ```

2. Update the **Core Value** statement:
   ```
   **Core Value:** A personal assistant Claude Code instance can reliably read and search the user's Outlook inbox, calendar, and Teams messages without ever sending, deleting, or mutating anything.
   ```

**Step D: Commit documentation changes.**

```bash
git add README.md SKILL.md CLAUDE.md
git commit -m "docs: add teams and copilot-summary subcommand documentation

Updates README.md, SKILL.md, and CLAUDE.md to describe the two new
subcommands, their usage, response shapes, error codes, and limitations.
Per phase completion checklist: both docs describe every subcommand."
```

**Step E: Merge feature branch to main.**

```bash
git checkout main
git merge feat/teams-copilot-merge --no-ff -m "Merge feat/teams-copilot-merge: add teams + copilot-summary (PR #1 reworked)"
```

**Step F: Close PR #1 with a reference comment.**

```bash
gh pr comment 1 --body "Merged via feat/teams-copilot-merge branch (Option B from review). Applied both requested changes:
1. Reverted --profile logic — using --session-name only (D-04)
2. Split policy-search.json into policy-teams.json + policy-copilot.json (D-09)

Also fixed isTeamsLoginUrl duplication (now uses isLoginUrl from session.js).

Thank you for the contribution!"

gh pr close 1
```
  </action>
  <verify>
    <automated>cd /Users/username/outlook-web-skill && git branch --contains HEAD | grep -q "main" && grep -q "copilot-summary" README.md && grep -q "copilot-summary" SKILL.md && grep -q "Teams" CLAUDE.md && grep -q "policy-teams.json" README.md && grep -q "policy-copilot.json" README.md && grep -q "Teams mentions" SKILL.md && gh pr view 1 --json state -q '.state' | grep -q "CLOSED" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the string `node outlook.js teams`
    - README.md contains the string `node outlook.js copilot-summary`
    - README.md contains `policy-teams.json` and `policy-copilot.json` in project structure
    - README.md error codes table `SESSION_INVALID` row contains `teams, copilot-summary`
    - README.md limitations section contains `Teams ARIA` and `Copilot availability`
    - SKILL.md frontmatter description contains `Teams` and `copilot`
    - SKILL.md contains `### teams` and `### copilot-summary` sections
    - SKILL.md error codes table updated with teams and copilot-summary
    - CLAUDE.md project description contains `Microsoft Teams`
    - CLAUDE.md Core Value contains `Teams messages`
    - `git log --oneline -1` shows merge commit on main
    - `gh pr view 1 --json state -q '.state'` returns `CLOSED`
  </acceptance_criteria>
  <done>
    README.md, SKILL.md, and CLAUDE.md all document teams + copilot-summary subcommands.
    Feature branch merged to main. PR #1 closed with comment explaining the merge approach.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify merge, code correctness, and policy file integrity</name>
  <files>lib/run.js, lib/teams.js, lib/copilot.js, outlook.js, policy-teams.json, policy-copilot.json, policy-search.json</files>
  <action>
Present the following verification steps to the user for manual confirmation:

1. Run `git log --oneline -5` to see the merge commit and preceding commits
2. Run `node -e "require('./lib/teams')"` to verify the teams module loads without error
3. Run `node -e "require('./lib/copilot')"` to verify the copilot module loads without error
4. Run `grep -c "session-name" lib/run.js` — should return 3
5. Confirm `policy-teams.json` does NOT contain "keyboard" (read-only teams, D-02)
6. Confirm `policy-copilot.json` contains "keyboard" (copilot needs it, D-07)
7. Confirm `policy-search.json` contains "dialog" and does NOT contain "keyboard" (unchanged from main)
8. Check PR #1 at https://github.com/gabeosx/outlook-web-skill/pull/1 — should be closed
9. Optionally: run `node outlook.js teams` against a live Teams session to test functionality
  </action>
  <verify>
    <automated>cd /Users/username/outlook-web-skill && node -e "require('./lib/teams')" && node -e "require('./lib/copilot')" && echo "Modules load OK"</automated>
  </verify>
  <done>User confirms the merge is correct, modules load, policy files have proper action restrictions, and PR #1 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy | SKILL.md triggers control when the calling LLM invokes this skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | SKILL.md description | mitigate | SKILL.md must include "NEVER use for sending, replying, deleting" in the description to prevent calling LLMs from attempting write operations. Verified: the updated description includes this constraint. |
| T-01-06 | Tampering | README.md | accept | Documentation is public. No secrets or credentials are included. |
</threat_model>

<verification>
1. `grep -q "copilot-summary" README.md && echo ok` returns ok
2. `grep -q "copilot-summary" SKILL.md && echo ok` returns ok
3. `grep -q "Teams" CLAUDE.md && echo ok` returns ok
4. `gh pr view 1 --json state -q '.state'` returns CLOSED
5. `git branch -d feat/teams-copilot-merge 2>/dev/null; echo ok` (branch merged, can be deleted)
</verification>

<success_criteria>
- All three documentation files (README, SKILL.md, CLAUDE.md) describe every subcommand including teams and copilot-summary
- Feature branch merged to main with no-ff merge commit
- PR #1 closed with explanatory comment
- Human verification confirms code loads and policy files are correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-review-critique-and-potentially-merge-this-pr-https-github-c/01-02-SUMMARY.md`
</output>
