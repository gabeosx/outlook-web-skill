---
context: research
phase: null
task: 11
total_tasks: 11
status: in_progress
last_updated: 2026-04-10T17:18:06.152Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: STALE_REF_DESTRUCTION — Outlook web contains unlabeled interactive elements that perform destructive actions (delete, archive). Clicking element refs from a stale snapshot caused an email deletion. Always re-snapshot before clicking, and NEVER operate without `AGENT_BROWSER_ACTION_POLICY` set to `default: deny` before the browser launches.

**Do not proceed with any browser automation until the above is acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| STALE_REF_CLICK | Clicked `e108` from snapshot N after DOM changed; ref remapped to an unlabeled delete affordance and deleted a real email | blocking | Always re-snapshot before clicking; never reuse refs across snapshots; Action Policy must be in place before browser opens |
| CHROME_FOR_TESTING | agent-browser defaults to its own "Chrome for Testing" binary; Entra Conditional Access blocks it on managed devices | advisory | Always pass `--executable-path` (OUTLOOK_BROWSER_PATH) + `--profile` (OUTLOOK_BROWSER_PROFILE) to all agent-browser invocations |

<current_state>
/gsd-new-project initialization is 10/11 tasks complete. ROADMAP.md was just created and approved by the user. One task remains before the project is fully initialized: commit ROADMAP.md + STATE.md and generate CLAUDE.md.
</current_state>

<completed_work>

- Task 1: Deep questioning (user interview) — Done
- Task 2: Write PROJECT.md — Done (commit fb371b1)
- Task 3: Workflow preferences + config.json — Done (commit a838368)
- Task 4: 4 parallel research agents (STACK, FEATURES, ARCHITECTURE, PITFALLS) — Done
- Task 5: Research synthesizer → SUMMARY.md — Done (commit b05df6a)
- Task 6: Write REQUIREMENTS.md (34 v1 requirements) — Done
- Task 7: Requirements approval from user — Done
- Task 8: Commit REQUIREMENTS.md — Done (commit 7e93a9f)
- Task 9: Create roadmap via gsd-roadmapper agent — Done (ROADMAP.md written)
- Task 10: Roadmap approval from user — Done (user said "yes")
</completed_work>

<remaining_work>

- Task 11: Commit ROADMAP.md + STATE.md + generate CLAUDE.md
</remaining_work>

<decisions_made>

- **Real Chrome/Edge required** — agent-browser's default "Chrome for Testing" is blocked by Entra Conditional Access on managed devices; skill requires OUTLOOK_BROWSER_PATH (executable) + OUTLOOK_BROWSER_PROFILE (profile dir)
- **Action Policy before browser launch** — unlabeled destructive elements in Outlook UI proved this is non-negotiable; policy must be set via AGENT_BROWSER_ACTION_POLICY before any browser process starts
- **data-automationid over refs** — refs are ephemeral and re-number after DOM changes; data-automationid values are stable and descriptive; skill implementation must use these
- **Phase 0 is exploration-only** — no code written until verified selectors exist in references/outlook-ui.md
- **6-phase roadmap** — Phase 0 (accessibility research) → Phase 1 (auth scaffold) → Phase 2 (search) → Phase 3 (read) → Phase 4 (digest) → Phase 5 (packaging)
- **34 v1 requirements** — grew from 28 original: added CFG-03/04 (browser config), PKG-04–07 (LLM reference docs), tightened SAFE-01, updated AUTH-02/SRCH-02/READ-01–02
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` — project overview, core value, constraints
2. `.planning/REQUIREMENTS.md` — 34 v1 requirements; note CFG-03/04 and updated SAFE-01 added this session
3. `.planning/ROADMAP.md` — 6-phase roadmap; approved and ready to commit
4. `.agents/skills/agent-browser/SKILL.md` — browser automation tool; critical for Phase 0 and Phase 1

## Critical Anti-Patterns (do NOT repeat these)

- **STALE_REF_CLICK**: Never click element refs without re-snapshotting first. Refs re-number after any DOM change. Outlook has unlabeled destructive affordances. → Structural mitigation: Action Policy (`default: deny`) must prevent click/fill except during `auth`.
- **CHROME_FOR_TESTING**: Never let agent-browser use its default binary on this project. → Always pass `--executable-path "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"` and `--profile ~/Library/Application\ Support/Google/Chrome/Profile\ 1`.

## Infrastructure State

- Git repo: `/Users/username/outlook-web-skill/`
- agent-browser: installed via Homebrew (v0.23.4)
- Real Chrome profile: `~/Library/Application Support/Google/Chrome/Profile 1` (confirmed working with Entra)
- `.planning/config.json`: YOLO mode, coarse granularity, parallel execution, balanced model profile

<context>
We just completed /gsd-new-project initialization. The requirements grew from 28 to 34 based on two key discoveries: (1) Entra Conditional Access blocks Chrome for Testing, requiring real browser config; (2) Outlook web has unlabeled destructive UI elements, requiring Action Policy before browser launch. The 6-phase roadmap is approved. One commit away from being fully initialized and ready to plan Phase 0.
</context>

<next_action>
Commit ROADMAP.md + STATE.md:
  node ".claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: add roadmap" --files .planning/ROADMAP.md .planning/STATE.md

Then generate CLAUDE.md to complete /gsd-new-project initialization.
</next_action>
